# Filecoin node API info (Required)
# For calibration testnet, use: FULLNODE_API_INFO=https://api.calibration.node.glif.io
FULLNODE_API_INFO=

# Lighthouse settings (Required only if using lighthouse as buffer for testnet)
# Get your API key from: https://docs.lighthouse.storage/lighthouse-1/how-to/create-an-api-key
LIGHTHOUSE_API_KEY=
# Default gateway URL, usually doesn't need to be changed
LIGHTHOUSE_DOWNLOAD_URL=https://gateway.lighthouse.storage/ipfs/

# Server configuration (Optional)
PORT=8000

# File paths (Optional)
# Default paths will be created if not specified
GENERATE_CAR_PATH=generated_car/
AGGREGATE_CAR_PATH=aggregate_car_file/